# Market Data Database (Parquet)

This directory contains the built parquet database. Query with DuckDB (`import duckdb`).

For table schemas, query patterns, and usage documentation, see [TABLES.md](TABLES.md).
